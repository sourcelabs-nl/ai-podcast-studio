## 1. Implementation

- [x] 1.1 Replace the end-of-week acknowledgement clause in the `fridayExtra` string in `buildHumorBlock()` (`ComposerUtils.kt`) with the conversational-only rule forbidding "Happy Friday" style shout-outs
- [x] 1.2 Update the `buildHumorBlock()` KDoc to record the listener feedback behind the rule
- [x] 1.3 Compile and run the full test suite (`mvn test`) — 874 tests pass
