Depends on `unify-episode-epilogue` being merged first: the epilogue built there is called from `PipelineRunner` for `Deliver`/`Review`/`Sandbox` outcomes.

## Phase A: RunSpec + PipelineRunner skeleton, migrate the 7 routes

- [ ] A.1 Define `RunPurpose`, `RunInput` (`Window`/`ArticleSet`/`Focus`), `ResumePoint` reuse, `RunOverrides`, `RunOutcome` (`Deliver`/`Review`/`Sandbox`/`Transient`), and `RunSpec` types, and verify with unit tests constructing one of each `RunInput`/`RunOutcome` variant
- [ ] A.2 Implement `PipelineRunner` supporting only `purpose = REGENERATE`, delegating every other purpose to the existing `PodcastService` code paths unchanged, and verify with a unit test that a `REGENERATE` `RunSpec` produces the same result `runRegeneration` produces today
- [ ] A.3 Migrate `regenerateEpisodeAsync` to build a `RunSpec` and call `PipelineRunner.run`, and verify: MockK unit test asserting identical behavior to before (composition-only, no `lastGeneratedAt` update, `EvaluationRun` recorded when `bypassLlmCache=true`), `mvn test` passes, app restarted (`./stop.sh && ./start.sh`), live check by calling `POST .../episodes/{id}/regenerate` against a real episode and confirming a new episode is produced
- [ ] A.4 Extend `PipelineRunner` to support `purpose = RETRY`, migrate `doRetry`/`retryEpisode` onto it, and verify: MockK unit tests for all three resume points (`FULL_PIPELINE`, `COMPOSE`, `POST_COMPOSE`), `mvn test` passes, app restarted, live check via `POST .../episodes/{id}/retry` on a FAILED episode
- [ ] A.5 Extend `PipelineRunner` to support `purpose = FOCUS` and `purpose = RECOMPOSE`, migrate `runFocusGenerationPipeline`/`composeAndFinalizeFocusEpisode` and `runFeedbackRecompose` onto it, and verify: MockK unit tests for focus generation and feedback recompose, `mvn test` passes, app restarted, live check via manual generation with a `focus` string and via the focus recompose endpoint
- [ ] A.6 Extend `PipelineRunner` to support `purpose = SCHEDULED`/`MANUAL`/`RERUN`, migrate `runGenerationPipeline`, `generateBriefing`, `generateBriefingAsync`, and `rerunEpisodeAsync` onto it, and verify: MockK unit tests covering all four call sites, `mvn test` passes, app restarted, live check by triggering a manual generation end to end
- [ ] A.7 Extend `PipelineRunner` to support `purpose = PREVIEW` with outcome `Transient`, migrate `previewBriefing`/`LlmPipeline.preview` onto it, and verify: MockK unit test asserting nothing is persisted, `mvn test` passes, app restarted, live check via the preview SSE endpoint
- [ ] A.8 Delete the now-unused `PodcastService` private methods (`runGenerationPipeline`, `runFocusGenerationPipeline`, `composeAndFinalizeFocusEpisode`, `runFeedbackRecompose`, `doRetry`, `runRegeneration`) and confirm `mvn test` still passes with no dead-code warnings

## Phase B: RunConfig, overrides, snapshot persistence, Sandbox outcome, purpose filtering

- [ ] B.1 Add migration `V82__add_episode_purpose_and_run_config.sql` adding `episodes.purpose` (backfilled: `LEGACY` for existing non-focus episodes, `FOCUS` for existing focus episodes) and nullable `episodes.run_config_json`, and verify `mvn test` runs the migration successfully against a copy of the production schema
- [ ] B.2 Implement `RunConfig` (app defaults + podcast settings + run overrides, resolved once per run) and `RunConfig.resolve`, and verify with unit tests covering all three precedence layers for each overridable field (model per stage, reasoning effort per stage, provider sort/preferred min throughput, target words, research budget, bypass-cache flag)
- [ ] B.3 Change `ModelResolver.resolve` to take `RunConfig` instead of `Podcast`, update all call sites, and verify `mvn test` passes with unit tests asserting run overrides take precedence over podcast settings over app defaults
- [ ] B.4 Thread `RunConfig` into `OpenRouterRouting`, the composers, and the research stage in place of direct `Podcast` reads, and verify with unit tests that a provider-sort/throughput override changes the request the composer sends
- [ ] B.5 Persist the resolved `RunConfig` snapshot to `episodes.run_config_json` at the point `PipelineRunner` finalizes a non-transient run, and verify with a unit test that the persisted snapshot matches the `RunConfig` the run actually used
- [ ] B.6 Implement the `Sandbox` outcome (no TTS, never publishes, no `lastGeneratedAt` update, no article consumption, epilogue still runs), and verify with a unit test covering each of those four guarantees
- [ ] B.7 Add `purpose != 'EXPERIMENT'` filtering to `EpisodeRepository.findLatestCoveringByPodcastId` (`EpisodeRepository.kt:40`) and `EpisodeWindowResolver.findLatestCoveredWindowEnd` (`EpisodeWindowResolver.kt:215`), and verify with unit tests that an experiment episode is ignored by both
- [ ] B.8 Add `purpose != 'EXPERIMENT'` filtering to the episode-list query/endpoint and the public feed query, and verify with unit tests that an experiment episode does not appear in either
- [ ] B.9 Add `purpose != 'EXPERIMENT'` filtering to the dedup-history lookup and `EpisodeService.hasActiveEpisode`, and verify with unit tests that an experiment episode does not affect dedup history or block a new generation as "active"
- [ ] B.10 Add `purpose != 'EXPERIMENT'` handling to `AutoPublishListener`/`PublishingService` so an experiment episode is never auto-published, and verify with a unit test
- [ ] B.11 Run `mvn test`, restart the app, and live-check that a `Sandbox`-outcome run (e.g. by driving `PipelineRunner` with `purpose = EXPERIMENT` manually against a test podcast) does not appear in the episode list, the feed, or affect the schedule

## Phase C: Experiments API, comparison endpoint, two real A/B experiments

- [ ] C.1 Add `app.experiments.max-variant-repeats` config and the pre-flight cost guard rejecting an oversized request before any compose call, and verify with a unit test that an oversized request makes zero `PipelineRunner.run` calls
- [ ] C.2 Implement `POST .../episodes/{id}/experiments` (N variants x k repeats, each a `PipelineRunner` run with `purpose = EXPERIMENT`, `input = ArticleSet` from the source episode, `outcome = Sandbox`), and verify with a MockK unit test that N*k runs are made with the correct overrides
- [ ] C.3 Implement the comparison `GET` endpoint (per-variant judge score, cost, duration, compose call count, reasoning tokens, serving provider), and verify with a unit test against a fixture of completed experiment episodes
- [ ] C.4 Run `mvn test`, restart the app, and live-check the experiment endpoints end to end against a real episode with a small (e.g. 1 variant x 1 repeat) experiment
- [ ] C.5 Run the "compose provider sort by throughput vs. default" A/B experiment via the new API against a real podcast/episode, and record the result in `knowledge/` (entry + index update + `knowledge/log.md`) per the project's knowledge-bundle conventions
- [ ] C.6 Run the "compose reasoning effort low vs. medium" A/B experiment via the new API, and record the result in `knowledge/` (entry + index update + `knowledge/log.md`) per the project's knowledge-bundle conventions
